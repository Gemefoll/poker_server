openapi: '3.0.3'
info:
  title: Poker server
  version: '1.0'
servers:
  - url: https://api.server.test/v1
paths:
  /user/create:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [Name, Pass]
              properties:
                Name:
                  type: string
                Pass:
                  type: string
      responses:
        '201':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PairToken'
        '409':
          description: The name is already exist
        '422':
          description: The name or the pass doesn't allow
  /user/auth:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [Name, Pass]
              properties:
                Name:
                  type: string
                Pass:
                  type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PairToken'
        '409':
          description: The name doesn't exist
        '422':
          description: The name or the pass don't allow
  /user/me:
    get:
      security:
        - BearerAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PairToken'
        '401':
          description: Token isn't valid
      
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    PairToken:
      type: object
      required: [AccessToken, RefreshToken]
      properties:
        AccessToken:
          type: string
        RefreshToken:
          type: string