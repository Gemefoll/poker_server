server {
    listen       80;
    server_name  localhost;
    location /api/ {
        proxy_pass          http://backend:12345;
        proxy_http_version  1.1;
    }
    location ~ \.(gif|jpg|png)$ {
        root /data/card_img;
    }
}